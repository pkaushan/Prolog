count(N,Sum):- 
    N mod 2 =:= 1,  %Проверяем на нечетность, если нечетное, то рекурсивно считаем сумму цифр, =:= - арифметическое сравнение
    Num is N div 2 + 1, 
    Sum is (1+N)/2*Num. 
count(N,Sum):- 
    N mod 2 =:= 0, %Проверка на четность, если число четное, то берем предыдущее от него и рекурсивно суммируем цифры
    Num is N div 2, 
    Sum is N/2*Num.
